name: Patch Preview Cache V5

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Bump preview cache version
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('src/worker.js')
          s = p.read_text()
          s = s.replace('/resource-preview-v2.css?v=4', '/resource-preview-v2.css?v=5')
          s = s.replace('/resource-preview-v3.css?v=4', '/resource-preview-v3.css?v=5')
          s = s.replace('/resource-preview-v2.js?v=4', '/resource-preview-v2.js?v=5')
          s = s.replace('/resource-preview-v3.js?v=4', '/resource-preview-v3.js?v=5')
          p.write_text(s)

          q = Path('scripts/verify-attachment-preview.mjs')
          t = q.read_text()
          t = t.replace("/resource-preview-v2.js?v=4", "/resource-preview-v2.js?v=5")
          t = t.replace("/resource-preview-v3.js?v=4", "/resource-preview-v3.js?v=5")
          q.write_text(t)
          PY
      - name: Commit cache bump
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/worker.js scripts/verify-attachment-preview.mjs
          if git diff --cached --quiet; then
            echo "No cache changes needed"
            exit 0
          fi
          git commit -m "fix: activate sharp pdf preview zoom runtime"
          git push
